# Minimal env for local tests/dev
# Non-sensitive runtime config is stored in the settings DB.

JARVIS_CONFIG_URL=
JARVIS_APP_ID=llm-proxy
JARVIS_APP_KEY=

MODEL_SERVICE_TOKEN=
LLM_PROXY_INTERNAL_TOKEN=

# Optional: seed settings from env for quick local runs
# python scripts/seed_settings.py --force
# JARVIS_MODEL_BACKEND=VLLM
# JARVIS_MODEL_NAME=meta-llama/Llama-3.2-1B-Instruct
